apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: knowledge-spool
  namespace: ka0s-knowledge
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: nfs-client

