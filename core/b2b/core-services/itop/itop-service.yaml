apiVersion: v1
kind: Service
metadata:
  name: itop
  namespace: itop
  labels:
    app: itop
    core.kaos.io/type: finops
spec:
  type: ClusterIP
  externalIPs:
    - 192.168.1.240
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: itop
