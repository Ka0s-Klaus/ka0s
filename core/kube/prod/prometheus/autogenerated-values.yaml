
# This is an autogenerated file created by Terraform: You MUST NOT modify this file, modify values.yaml instead where you can override values set in this file.

environment: prod
is_stack: true

alertmanager:
  enabled: false

prometheus:
  annotations:
    downscaler/exclude: "true"
  prometheusSpec:
    storageSpec:
      volumeClaimTemplate:
        spec:
          storageClassName: regional-ssd
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 12Gi

kubeStateMetrics:
  annotations:
    downscaler/exclude: "true"
