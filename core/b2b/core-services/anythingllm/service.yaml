apiVersion: v1
kind: Service
metadata:
  name: anythingllm
  namespace: anythingllm
  labels:
    app: anythingllm
    managed-by: ka0s-core-cd
spec:
  type: ClusterIP
  selector:
    app: anythingllm
  ports:
    - name: http
      port: 3001
      targetPort: 3001
