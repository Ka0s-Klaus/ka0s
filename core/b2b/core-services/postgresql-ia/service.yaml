apiVersion: v1
kind: Service
metadata:
  name: postgresql-ia
  namespace: postgresql-ia
  labels:
    app: postgresql-ia
spec:
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: postgresql-ia
  type: ClusterIP
